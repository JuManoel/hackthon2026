name: verify-implementation
description: Validate that new code respects repository rules.

Steps

run lint

run typescript check

ensure no magic literals

ensure labels are used

if violations exist fix them
